{
  "name": "iiitn-streaming-platform",
  "version": "1.0.0",
  "description": "IIITN Streaming Platform - Fantasy League & Watch Party",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../fantasy-league && npm install",
    "build": "cd fantasy-league && npm install && npm run build",
    "postinstall": "cd backend && npx prisma generate",
    "start": "cd backend && node server.js",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd fantasy-league && npm run dev",
    "seed": "cd backend && node seed.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
